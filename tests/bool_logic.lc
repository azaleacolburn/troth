fn TRUE la.lb.a;
fn FALSE la.lb.b;

// Logical AND
fn && la.lb.((a b) a);

// Logical OR
fn || la.lb.((a a) b);

// Logical NOT
fn ! la.lb.lc.((a c) b);

// Expression-based if statement
// Params: condition, true case, false case
fn IF_ELSE lcond.lthen.lelse.((cond then) else);

IF_ELSE (&& (! FALSE) TRUE) first second
